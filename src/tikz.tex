\usepackage{tikz}
\usetikzlibrary{
	arrows,
	arrows.meta,
	calc,
	graphs,
	patterns,
	positioning,
	shapes,
	decorations.pathmorphing,
}

% LABELS
\tikzset{label/.style={
	draw=none,
	sloped,
}}

\tikzset{label above/.style={
	label,
	midway,
	above=-1mm,
}}

\tikzset{label below/.style={
	label,
	midway,
	below=-1mm,
}}

%DEFAULT STYLE
\tikzset{
	>=Latex
	,very thick
	,every node/.style={
		draw 
		,circle
		,inner sep=0mm
		,minimum size=5mm
		,very thick
		,font=\small
	}
}